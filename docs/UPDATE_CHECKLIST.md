# Update checklist

1. Review new and changed rules in the Node markdownlint changelog.
2. Validate each style with the library and `markdownlint-cli`.
3. Test installed-package `extends` from a packed consumer.
4. Review the generated-style relaxations individually.
5. Inspect packed JSON subpaths and public declarations.
